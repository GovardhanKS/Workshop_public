scikit-learn>=1.4
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
streamlit>=1.32
requests>=2.31
fpdf2>=2.7
python-dotenv>=1.0  # loads .env for LLM_MODE etc. outside docker-compose (see agents/llm.py)

# LLM backend for LLM_MODE=llm (Ollama / llama.cpp / Groq / HF Inference
# are all OpenAI-compatible endpoints) -- lightweight client, no local
# model weights.
openai>=1.0

# Production embedding/vector-store backend (EMBEDDING_BACKEND=hf) lives
# in requirements-hf.txt instead of here: sentence-transformers pulls in
# torch, which is a multi-GB install the default TF-IDF backend never
# needs. Install it only if you're actually switching backends:
#   pip install -r requirements-hf.txt
