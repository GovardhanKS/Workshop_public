# Optional MCP stdio server (mcp_server/server.py) -- only needed if you
# want to expose this project's tools to MCP clients/agents. Install
# alongside requirements.txt, not instead of it:
#   pip install -r requirements.txt -r requirements-mcp.txt
mcp>=1.0
